# Signing key — secret, never commit. Keep in the district password vault.
key.pem
*.pem

# Original starter bundle — contains a copy of key.pem inside it; never commit/host.
hide-ai-mode-starter.zip

# macOS junk
.DS_Store

# Internal-only docs — kept locally, never published to the public repo.
CLAUDE.md
PROMPT.md

# Build output (regenerated by `npm run build`)
dist/

node_modules/
